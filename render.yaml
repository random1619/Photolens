services:
  - type: web
    name: photolens
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    branch: main
    envVars:
      - key: VITE_API_URL
        value: https://api.example.com
